<div class "title-container">
<h3>This is your Project's show page </h3>
</div>

<div>
<p>
<strong>Name: </strong>
<%= @project.name %>
</p>

<p>
<strong>Completion Status:</strong>
<%= @project.completion_status%>
</p>

</div>
<h4>Tasks:</h4>
<% @project.tasks.each do |task| %>
  <p>
  
    <strong>task:</strong>
    <%= task.description %>
    <%= task.schedule %>
    <%= task.completion_status %>
    <h4>By: <%=link_to task.user.username, user_path(task.user)%> </h4>

  </p>
<% end %>

<div>
<strong><%= link_to "Add a Task", new_project_task_path(@project), class: "btn btn-primary" %></strong>
</div>
  <br>
  
<%= link_to 'Edit', edit_project_path(@project), class: "btn btn-info" %> |

 <%= link_to 'Delete', project_path(@project),method: :delete, data: { confirm: 'Are you sure you want to delete?' }, class: "btn btn-info" %>
 <br>
 <br>
<%= link_to 'Back', projects_path, class: "btn btn-info" %>
 
